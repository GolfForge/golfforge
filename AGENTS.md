# AGENTS.md

This repo's agent context lives in [`CLAUDE.md`](CLAUDE.md) at the repo root. **Read that first.**

Kept for the cross-agent `AGENTS.md` convention; `CLAUDE.md` is the single source of truth.
The heavyweight reference material it used to hold now lives in `docs/` (see the doc map at the
top of `CLAUDE.md`): `worklog.md`, `backlog.md`, `ue5-cookbook.md`, `pipeline-data-contract.md`.
