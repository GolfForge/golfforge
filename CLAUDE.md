# CLAUDE.md — context for Claude (and other coding agents) in `golfsim`

If you're an AI assistant landing in this repo, read this file first. It's the cross-session,
cross-machine handoff document — Claude/Cowork sessions don't sync between machines, so this
file (committed to git) is how the Mac-side and Windows-side sessions stay coherent.
**Read on start, update before stop, commit before exit** (see the session-end checklist below).

This file stays slim on purpose. The heavyweight reference material lives in `docs/` and is read
**on demand** — pull these in only when the task calls for it:

- **Linear** ([linear.app/golfsim](https://linear.app/golfsim)) — the prioritized work queue. Every ticket has goal, plan, done-when, dependencies, and files to touch. Each is also tagged `machine/windows` / `machine/mac` / `machine/either` so you can filter to what the current machine can actually do.
- `docs/worklog.md` — what's been done (summarized history). This is the "shipped features" reference.
- `docs/ue5-cookbook.md` — UE5/MCP/PCG pitfalls + recipes + Fab asset list (read when you hit an engine issue)
- `docs/pipeline-data-contract.md` — the `courses/<id>/` file-shape contract
- `docs/plan.md` — full project plan, MVP ladder, decisions
- `docs/event-protocol.md` — the event envelope every driver implements
- `docs/windows-setup.md` — Windows prerequisites + UE5 project creation + MCP wiring
- `README.md`, `pipeline/README.md` — human-facing orientation + pipeline quickstart

---

## Project in one paragraph

`golfsim` is an open-source, cross-platform golf simulator. The competitive thesis is three-fold: (1) AI-assisted course pipeline that ingests open LIDAR + OSM data so the community can produce courses 10x faster than existing community-course tools allow; (2) walking/treadmill integration via BLE FTMS that adds a fitness tier to sim-golf (sit-down play stays a first-class option); (3) genuinely cross-platform — Windows / Mac / Linux desktop with iPad as a future mobile tier, all from a single UE5 codebase.

## Architecture invariants — do not break these

1. **Monolithic per-platform binary.** Each platform ships as one executable containing the sim, the renderer, and the platform-appropriate hardware drivers. No services, no IPC for end users. Drivers go in via CoreBluetooth (Apple) / Windows.Devices.Bluetooth (Windows) / BlueZ (Linux). Multi-machine setups are developer-only.
2. **In-process normalized event bus.** All hardware drivers (walking sensor, launch monitor, HR strap, manual UI) publish typed events of the same shape to an in-process pub/sub. The sim subscribes. Multiplayer is the same envelope serialized over the network. The shape is the contract — see `docs/event-protocol.md`.
3. **Course pipeline is decoupled from the engine.** The Python pipeline in `pipeline/` runs anywhere; it produces UE5-import-ready PNGs in `courses/<id>/`. The engine ingests those files. Pipeline and engine never share runtime state. (File shapes: `docs/pipeline-data-contract.md`.)
4. **AGPL-3.0 + commercial dual-license.** The repo is AGPL-3.0 (`LICENSE`); closed-source/proprietary use needs a separate paid commercial license (`COMMERCIAL.md`). New code in the repo MUST be AGPL-3.0-compatible, and external contributions require a CLA so the project can keep offering commercial exceptions. Public-facing name: **GolfForge**.

## Where work happens (which machine does what)

| Machine | Owns |
|---|---|
| Mac mini M5 | Python pipeline development. All docs. Future BLE driver prototypes via `bleak`. Future Mac/iOS UE5 builds (Apple toolchain requires Mac). |
| Windows PC | UE5 development for Windows/Linux build targets. MCP-driven editor automation. C++ engine work. |
| Both | The git repo. Anything committed is visible to the other machine on next `git pull`. |

## Conventions

- **Git:** main branch only for now (single contributor). Conventional-style commit messages welcome but not enforced. Use `git lfs` — `.gitattributes` declares LFS rules for `*.uasset`, `*.umap`, `*.png` in `engine/` and `courses/`.
- **Python:** the pipeline uses stdlib + `requirements.txt`; run `./setup.sh` to create the venv (auto-detects `uv`, falls back to `python3 -m venv`). Engine-side helper scripts (`engine/scripts/*.py`) run via `execute_unreal_python` in the UE5 embedded interpreter and are **stdlib-only** — no system venv on Windows.
- **UE5:** project files live in `engine/`. Keep one `.uproject` there. Marketplace/Fab assets are gitignored & re-downloadable per machine — current asset list + Fab-plugin notes are in `docs/ue5-cookbook.md`.
- **Pipeline outputs:** committed assets in `courses/<id>/` are reference data, not throwaway artifacts. Shapes + how to consume each file: `docs/pipeline-data-contract.md`. `osm_raw.json` and `dem.tif` are gitignored intermediates.
- **No emojis** in files unless explicitly requested.
- **No new markdown files** unless they add structural value. Update existing docs rather than creating siblings.

## Current status

- **Latest:** **GOL-38 ground-roll: multi-bounce / skip model — driver shows 3 visible hops then a short roll** (2026-06-01, Windows). Replaced GOL-9's flat friction-slide with an analytical bounce phase: at landing, descent + spin attenuation reduce `Vh` once (initial scrape); then up to 6 parabolic hops where vertical `Restitution` (reinterpreted as **vertical coefficient of restitution**) launches `Vv` upward and a new `BounceHorizontalKeep` field decays `Vh` per bounce, stopping when `VvOut < 0.40 m/s`; finishes with the existing constant-friction roll. Hop parabolas sample into `RollSamples` with non-zero Z (the visualizer already lerps Z so the ball arcs in playback). Headline driver case (74.6 m/s, 10.9°, 2686 rpm): **hops=3, peaks (m)=2.09, 0.25, 0.03; total=280.7 yd ✓** matches GOL-9's ~280 yd target. Per-surface tables recalibrated; `FSurfaceRoll.Restitution` semantics flipped (was horizontal retention into roll — now vertical COR per bounce; `BounceHorizontalKeep` took over the horizontal job). API surface unchanged for callers (`FGroundRollResult` fields keep their meanings; `RollDistanceM` now covers hops+roll). New `Golfsim.GroundRoll.DriverHops` test asserts hop count ≥ 2 + strictly-decreasing peaks; **29/29 automation tests pass** (was 28/28 plus the new one). Files: `Physics/GroundRoll.{h,cpp}`, `Tests/GroundRollTests.cpp`, `CLAUDE.md`, `docs/worklog.md`. **GOL-38 DONE pending commit + push. GOL-93 down to 2 children: GOL-39 (coefficient + spin-back tuning) + GOL-40 (course-side lie detection).**
- **Prior:** **GOL-108 closed end-to-end — pipeline + UE5 both reflect widened bbox; all 18 Black hole endpoints on landscape** (2026-06-01, Windows half). Picked up Mac's `fc2088f` (pipeline re-cook for `-73.4555, 40.7423, -73.4345, 40.7571`). Bumped `BBOX_WGS84` in **both** `engine/scripts/build_water_actors.py` AND `engine/scripts/build_hole_markers.py` (the two share a lon/lat → world affine; cross-referenced in each file's comment). Two-pass Landscape > Manage > Import: heightmap.png with **`RelativeScale3D.Z` bumped 8.4167 → 10.1167** (Z DID change this time — elev range grew 43.1m → 51.8m as the wider bbox now captures more of the surrounding hills; X/Y still 100 per GOL-85's finding), then 7 splat layers via the Layers array. Re-cooked water (10 built, 0 Z_ESTIMATED, Z values shifted ~20% deeper consistent with the stretched Z scale) + PCG (30,402 trees in gate band [25K, 60K]). **Programmatic verify via the committed `build_hole_markers.py` dev tool: 18 Black holes scanned, 0/36 endpoints off-bbox** — every one of the 5 prior offenders (Black 1 Tee, 9 Tee, 10 Green, 11 Tee, 18 Green) now sits inside the landscape. `save_dirty_assets` clean before markers spawned (on-disk umap markers-free). **GOL-33 → GOL-85 → GOL-108 round-trip closed.** Files: `engine/scripts/build_water_actors.py`, `engine/scripts/build_hole_markers.py`, `engine/Golfsim/Content/Maps/GolfForgeDemoBlack.umap`, `CLAUDE.md`, `docs/worklog.md`. **GOL-108 DONE pending commit + push.**
- **Prior:** **GOL-85 UE5 landscape post-GOL-33 reconciled — ticket premise corrected, re-import + water re-cook + PCG re-scatter** (2026-06-01, Windows). Read-only landscape inspection on the open `GolfForgeDemoBlack` returned `loc=(-100800,-100800,0) scale=(100.0000, 100.0000, 8.4167)` — confirming what `docs/windows-setup.md:171`, `pipeline/README.md:86`, and `docs/pipeline-data-contract.md:10` all already document: only Z is data-driven (`ue5_z_scale_pct` from `heightmap.json`); X/Y stay at default 100. The landscape has always been a default-stretched 2016m × 2016m (heightmap PNG fills it uniformly), **never** the physically-sized 1404m × 1400m the ticket assumed. The new heightmap/splat PNGs fill the same landscape with bbox content shifted east — hole 8 appears at the eastern edge automatically, **no `RelativeScale3D` change needed**. Did the work that actually was needed: (a) re-imported `heightmap.png` + 7 splat layers (`splat_{fairway,green,bunker,rough}.png` + `layer_{tee,trees,cart_path}.png`) via Landscape > Manage > Import (two passes — heightmap then layers — per `docs/ue5-cookbook.md:73`); weightmap re-import auto-triggers PCG regen per cookbook. (b) Bumped `engine/scripts/build_water_actors.py` `BBOX_WGS84` maxlon `-73.4374` → `-73.435` and re-ran in-editor: **10 water bodies built** (2 new `Water_544173094`/`Water_797200850` in the eastern bbox extension), 0 Z_ESTIMATED. (c) Re-ran `scatter_full_landscape.py` (MODE A then MODE B report): **30,437 PCG tree instances**, gate band [25K, 60K] PASS. (d) `save_dirty_assets` clean (no SavePackage warnings). PIE-verified Black hole 8 paint (fairway/green/bunkers) on the eastern landscape edge where it was previously empty. **Verify uncovered a bigger pipeline-side bbox miss — GOL-108 filed.** Spawned visual hole-end pillars at every Black hole's tee + green (from `hole.geojson`); **5 endpoints landed off the landscape**: Black 1 Tee (49 m W), Black 9 Tee (7 m E + 27 m N), Black 10 Green (168 m N), Black 11 Tee (182 m N), Black 18 Green (83 m W). GOL-33 widened east for Black 8 specifically — N + W edges were never audited. Proposed corrected bbox `-73.4555, 40.7423, -73.4345, 40.7571` (~50 m buffer past each at-risk endpoint). GOL-108 carries the full table + re-cook + re-import recipe (`machine/mac` first then `machine/windows`). GOL-85's Windows-side landscape work stands — the leftover is purely pipeline-side. (GOL-108 also folds in the stale `pipeline/tests/test_build_splatmap.py:34` `BBOX_GOLFFORGE_DEMO` fixture bump.) **GOL-85 DONE pending commit + push; GOL-108 next on Mac.**
- **Prior:** **GOL-33 pipeline polish — `hole.geojson` sidecar + bbox-extended demo course (18/18 Black holes)** (2026-05-31, Mac). Three findings from the Overpass audit: (1) **rasterization is exact** (38 fairway ways → 39 components, 25 greens → 25, 83 bunkers → 83, 66 tees → 66 via BFS); (2) `golf=hole` linestrings (32 of them, carrying `par`/`handicap`/`ref`/`name`/`golf:course:name`) were being dropped on the floor; (3) **Black hole 8 was clipped by the bbox** — it sits on the eastern side (lon `-73.43616..-73.43557`), 138m east of the prior maxlon `-73.4374`. Fixes: (a) new `FEATURE_LAYERS["hole"]` entry (`geom=line`, `emit_geojson=True`, `skip_raster=True`) emits `hole.geojson` with full `osm_tags` preserved (**unblocks GOL-69** scorecard / hole selector / opening fly-in on any course where OSM has `golf=hole`); (b) `cart_path` extended to `highway=service` + Overpass query updated; (c) **bbox widened east** `-73.4374` → `-73.4350`, demo course re-cooked → **18/18 Black holes including hole 8 (par 3, handicap 14)**, 13 Green, 2 Red = 33 hole features. **35/35 pytest** (new test freezes metadata-round-trip + skip-raster + no-splat-contamination). `pipeline/build_splatmap.py` + tests + `docs/pipeline-data-contract.md` + `docs/plan.md` (bbox bump) + `.gitignore` + regenerated `courses/golfforge-demo-black/*`. **Windows-side follow-up resolved by GOL-85 above.**
- **Prior:** **🚀 v0.0.1-alpha PUBLIC LAUNCH — repo is now public** (2026-05-31, Both). [github.com/GolfForge/golfforge](https://github.com/GolfForge/golfforge) flipped public, v0.0.1-alpha pre-release published with both `GolfForge-macos-arm64.zip` (692 MB) and `GolfForge-windows-x64.zip` (759 MB). GOL-49 DONE. **Identity hardening 100% done** (GOL-76 → 77 → 78 → 79 → 80 → 81 all landed). Live in alpha: practice range, OpenFlight LM, settings menu, manual-shot dialog, ball-flight solver, ground roll, main menu. Known v1 trade-offs in README "Coming soon": no courses yet, no walking, no MetalFX (TSR-only on Mac), unsigned binaries both platforms.
- **Prior:** **v0.0.1-alpha cross-platform Release complete — Win64 build attached** (2026-05-31, Windows). Post-rewrite re-clone via HTTPS; restored gitignored per-machine content (MCP plugin + Fab + Megaplant + PC3D + `__External__` — ~2.3 GB) from the `.prerewrite` backup. Editor build clean (74 s, 113 modules) + **28/28** automation tests green. First cook crashed on missing textures (all 5 per-machine Content dirs absent on the fresh clone); second cook clean with the Mac-corrected flag set adapted for Win64 (`-package -prereqs -manifests -unversionedcookedcontent -nodebuginfo`, range-only via `-map=/Game/Maps/PracticeRange`). `Golfsim.exe` (1.1 GB) end-to-end smoke-test verified (main menu / range / settings / quit); `GolfForge-windows-x64.zip` (759 MB) uploaded to v0.0.1-alpha alongside `GolfForge-macos-arm64.zip` (692 MB) — **cross-platform release ready to publish + flip the repo public**. GOL-78 Done on both machines (`--local` scope, preserves personal identity in other repos). Still open under GOL-49: committed cross-platform packaging script + OpenFlight-from-package smoke test.
- **Prior:** **Pre-public-launch identity + branding scrub mostly DONE; README ready for public eyes** (2026-05-31, Mac). Identity sequence GOL-76 → 77 → 78 → 79 → 80 all landed: domain `golfforge.org` reserved at Cloudflare + Google Workspace mailbox + aliases live; GitHub org GolfForge created + repo transferred + renamed to `golfforge`; git author identity per-repo set to `Fran <fran@golfforge.org>` (`--local` scope, personal identity preserved globally); current-repo personal-info scrub clean (GOL-79); git history rewrite via `git filter-repo --mailmap` (GOL-80) — needed two passes to catch both `fran.mazzoni@gmail.com` AND the `1002620+pucho@users.noreply.github.com` noreply form for web-UI commits. Force-pushed; `~/code/golfsim.prerewrite` kept as rollback. Then a GSPro/competitor-name scrub + "couch sim" reframe + OpenFlight hyperlinks added across docs. README got a "Try it" section (Mac/Windows install with Gatekeeper / SmartScreen workarounds for unsigned builds) + "Coming soon" section (playable round, practice modes, walking integration). New GOL-83 filed for Apple Developer Program enrollment + notarization — deferred until revenue/appetite justifies $99/yr. **Identity hardening 95% done — only GOL-81 (GH 2FA hardening) left, independent.**
- **Prior:** **Identity hardening tickets filed** (2026-05-31, Mac). 6 sibling tickets for pre-public-launch identity protection: GOL-76 reserve `golfforge.org` + Google Workspace ($94 first year, Urgent + blocks the rest), GOL-77 GolfForge GH org + repo transfer (Urgent), GOL-78 git author identity → `fran@golfforge.org`, GOL-79 scrub maintainer's personal identifiers (legacy GH handle, Mac username, personal email) from current repo, GOL-80 git history rewrite via `git filter-repo`, GOL-81 GH 2FA + profile hardening.
- **Prior:** **Trademark scrub (GOL-20)** (2026-05-30, Mac). Replaced "Bethpage" / "BethPage" / "BethPageBlack" with "GolfForge Demo" / "GolfForgeDemo" / "GolfForgeDemoBlack" across 24 files; renamed `courses/bethpage-black/` → `courses/golfforge-demo-black/` (via `git mv`, history preserved); deleted the week-1 `courses/pebble-beach-test/` scaffold. Geographic bbox + cooked data preserved (public-domain LIDAR + OSM isn't trademarkable — only the name was). 34/34 tests still pass. **Windows side on next pull:** rename `BethPageBlack.umap` → `GolfForgeDemoBlack.umap` via Content Browser (proper Object Redirector path, not bash mv), then Fix Up Redirectors + save the umap. Engine code references to the level path are already updated by the scrub.
- **Prior:** **GOL-49 macOS package FUNCTIONAL + plugin gating** (2026-05-30, Mac). First successful Mac `BuildCookRun` produced a launchable `Golfsim.app` — main menu, range, settings, upscaler (TSR-only on Mac, correct degradation), quit all working. Apple Silicon M4 + macOS 15.5. Whole C++ codebase compiled clean for Mac arm64 first try (67 s build). Plugin gating: DLSS / StreamlineDLSSG / StreamlineCore / StreamlineDeepDVC / XeSS now `"Optional": true` + `"SupportedTargetPlatforms": ["Win64"]` so Mac skips them silently while Windows behavior is unchanged. The corrected `BuildCookRun` (source-of-truth for the still-pending `engine/scripts/package.sh`) adds `-package -prereqs -manifests -unversionedcookedcontent -nodebuginfo` to the previous flag set — without `-package` the binary expects `.uproject` at its source-tree relative path. **Mac perf baseline:** 15-20 FPS at native 1080p TSR-only on M4 base; GOL-72 (Mac DPI fix → render at native 4K) + GOL-64 (MetalFX upscaling) are the levers to get it playable. Full GOL-49 still needs the committed packaging script + GitHub Release attachment; the technical blockers are cleared. Earlier same session: GOL-50 (OpenTopo key externalized, 34/34 tests).
- **Prior:** **Main menu + DPI-aware native-res render; first Windows package verified** (2026-05-29, Windows). A pure-C++ `UMainMenu` greets on launch over the soft-blurred live range (**Range** dismisses to the range / **Play Course** disabled with "Coming soon" / **Exit** quits) — instant entry, gameplay gated until Range. `bAllowHighDPIInGameMode=True` now renders at the **physical panel resolution** by default (fixed a 4K@150% desktop rendering at 1440p; never upsizes past native): **Resolution dropdown = output res**, **Upscale Mode = the render-scale FPS lever**. Borderless greys the resolution picker; credits carry Epic's exact UE notice. **First Win64 Development package (GOL-49) built + verified on a 4K machine** — range-only cook (`-map=/Game/Maps/PracticeRange`; only `PracticeRange.umap`), **28/28 tests**; macOS build + committed path-agnostic script still open, FSR → GOL-62. Same session also shipped the **Settings + credits + TSR/DLSS/XeSS upscaler** menu (GOL-52/59/53): Esc/Tab-toggled **centered settings modal** on the range HUD: resolution / window mode / quality via `UGameUserSettings` (persisted); an **Upscaler** selector — **TSR + DLSS + XeSS**, each with its own quality-tier labels — that's **cvar-driven (no plugin module dependency)**, all in the TSR slot (`r.AntiAliasingMethod 4`+`r.TemporalAA.Upsampling 1`), one vendor at a time (DLSS gated to NVIDIA via `IsRHIDeviceNVIDIA()`, XeSS DP4a); a **Credits** screen (OSM/ODbL + Unreal EULA + USGS — clears GOL-49's in-app attribution prereq); and a **Quit Game** button. New `GolfDisplaySettings.{h,cpp}` (pure, tested) + `SettingsMenu.{h,cpp}` (pure-C++ `UUserWidget`) via `AGolfRangeHUD`; `golfsim.SetResolution/SetQuality/SetUpscaler/Credits`. **28/28 tests**, PIE-verified — GOL-52/59/53 Done. **FSR deferred → GOL-62.** First Win64 Development package (GOL-49) building. Detail in `docs/worklog.md`.
- **Prior:** **Open-sourcing groundwork (GolfForge) — all P0 publish-blockers DONE** (2026-05-29, Windows). Repo is going public as **GolfForge** under **AGPL-3.0 + a paid commercial dual-license** (CLA-gated code contributions; Issues-on/PRs-closed at launch; manual GitHub Releases for macOS+Windows). Shipped: MIT→AGPL swap (`LICENSE`/`COMMERCIAL.md`; **invariant #4 updated — new code must be AGPL-compatible** — GOL-43); Fab/Megascans audit (already gitignored + clean history, no scrub — GOL-44); secret/PII full-history scan (clean — GOL-45); `ATTRIBUTION.md` for OSM ODbL + USGS/SRTM (GOL-46); community files + public README + `.github` templates/PR-auto-close (GOL-47); engine/scripts portability via `unreal.Paths` (GOL-60). Follow-ups: GOL-59 (in-app credits, blocks GOL-49), GOL-61 (CoC human-review). **Before the public flip:** reserve the GolfForge org/domain, then P1 GOL-49/50/51. Full detail in `docs/worklog.md`; backlog GOL-43..61 in Linear.
- **Earlier:** **GOL-9 ground roll + range follow camera DONE** (2026-05-28, Windows). Carry now becomes **total distance + final lie** via a pure-SI **analytical** ground-roll model (not Chaos): `Physics/GroundRoll.{h,cpp}` (`EGolfLie` + per-surface `FSurfaceRoll` table + `SimulateGroundRoll`) and `Physics/RangeSurface.{h,cpp}` (`ClassifyLie` mirroring `build_range_splatmap.py`), wired through a world-agnostic **`SurfaceProvider` seam** on the integrator (the range HUD registers one: launch-local SI → world → lie), plus a panel **Total (yd)** row. Trackman driver = carry 264 / total 280.5 yd; **24/24 tests** (6 new `Golfsim.GroundRoll.*`). The range also got a **Follow camera** (GOL-37: a chase `ACameraActor` via `SetViewTargetWithBlend` + right-click orbit; the ball now **launches from the floor**, not eye height; red landing-marker removed; no 16:9 letterbox). **Post-landing is a flat friction-slide for now (no real skips)** — follow-ups GOL-38 (multi-bounce), GOL-39 (physics refinement: coefficients/green spin-back/cross-surface/UDataAsset), GOL-40 (course lie from splatmap PNGs), GOL-41 (follow-cam UX), GOL-42 (range bunker/green). *Prior — **GOL-11 OpenFlight LIVE over Socket.IO; GOL-36 fixed** (2026-05-28): a pluggable LM framework under `Drivers/`: abstract `ULaunchMonitorDriver` + `ULaunchMonitorManager` (`UGameInstanceSubsystem`) that enumerates/selects the active driver (the seam a future settings UI + Square Omni/GOL-12 use), with `UOpenFlightDriver` the first concrete driver. OpenFlight is **Socket.IO (Engine.IO v4) over Flask-SocketIO**, so the driver speaks a minimal EIO4 layer on UE's `IWebSocket` (handshake `0`→`40`→ack, ping `2`/pong `3`, decode `42["shot",{shot,stats}]`). Real-schema parser: `club`, `launch_angle_vertical`/`_horizontal`, `spin_rpm`+`spin_axis_deg`→back/side split, launch-angle est-spin heuristic shown as `est` on the panel. HUD subscribes `shot.taken` so **any** producer (random/manual/LM) drives the panel; panel got a connection-status dot. Console: `golfsim.LMSelect/LMConnect/LMDisconnect/LMSimulate/LMTrigger`. AGPL-isolated (talk-over-socket only; `Drivers/README.md`). **GOL-36 was NOT an lws bug:** the "can't connect" was a Windows `localhost`→IPv6 `::1` mismatch vs the IPv4-only Flask dev server (binds `0.0.0.0`) — lws dialed a dead `::1` and surfaced a misleading `WSAENOTSOCK`/errno 10038; fixed by mapping `localhost`→`127.0.0.1` in `ResolveUrl`. Verified live (`golfsim.LMConnect` → mock shots → ball + panel; 17/17 tests). An experimental plain RFC 6455 `/feed` transport (simpler, no EIO4) is parked on branch `feature/openflight-raw-feed` pending a possible upstream PR; `main` ships Socket.IO so it works against every stock OpenFlight install. **Range UX landed with it:** a panel **Launch Monitor dropdown** (Off + drivers) connects/disconnects the active driver, a **Simulate Shot** button (shown when connected) round-trips `simulate_shot`, **Carry counts up** during ball flight, and our club selection **pushes to OpenFlight** (`set_club`) with naming normalized in the driver (our display names ↔ OpenFlight ids). Also fixed a one-shot panel-display lag by carrying the source shot's launch metrics on `FShotOutcomeEvent` (the integrator publishes the outcome before the HUD's own `shot.taken` ran). Earlier 2026-05-26: **GOL-8 manual-shot dialog DONE** — the first deliberate EventBus *producer*: a pure-C++ `UManualShotDialog : UUserWidget` (no WBP; `USpinBox` fields for ball speed/launch/backspin/sidespin/azimuth + a club dropdown + Fire). **M** toggles it on the range (auto-fire panel hides); Fire routes through the shared `AGolfRangeHUD::PublishShotTaken` (extracted from `FireRandom`) and publishes `FShotTakenEvent` with `Source="manual-shot-dialog"`, so it flies the entered shot (not random); picking a club autofills the spinners from the bag and the dialog shows the resolved carry/offline. Bottom-right anchored, fixed-width 1-decimal spinners (mostly stops the AutoSize panel jittering on slider drag; tiny residual drift accepted). Earlier 2026-05-26: **GOL-7 EventBus DONE** — the day-one in-process pub/sub (invariant #2): pure-C++ `FGolfEventBus` (typed channel per `EEventKind`, synchronous snapshot dispatch) wrapped by `UEventBusSubsystem : UGameInstanceSubsystem`; envelope + payload `USTRUCT`s in `Events/EventTypes.h` (`FGolfEvent` base + `FShotTakenEvent`/`FShotOutcomeEvent`, per `docs/event-protocol.md`). A built-in integrator subscriber turns `shot.taken` → `session.shot_outcome` via `GolfBallFlight::Simulate`; the range now **fires through the bus** (`AGolfRangeHUD::FireRandom` publishes, `OnShotOutcome` plays the ball + panel — no direct solver call), `golfsim.PublishTestShot` round-trips it, 4 new automation tests (11/11 pass headless). **Unblocks all 10 driver/sim tickets.** *Prior (2026-05-23, Windows): Practice Range R3 + R4 DONE.* **R3 trees:** range got its own PCG graph `/Game/PCG/PCG_TreeScatter_Range` (separate from GolfForgeDemo's to dodge the shared-density footgun), driven by `scatter_range_trees.py` (persistent, GenerateOnLoad). `build_range_splatmap.py` reworked from 504 m perimeter ring → tight 400×70-yd open lane framed by a 22-yd tree wall (trees ±35 yd off center). Perf: 4K had tanked to ~45 FPS (GPU/geometry-bound; 3D fairway grass was ~half the cost), so grass **removed from the range** (grass-output node deleted from `M_PracticeRange`; `build_range_material.py` defaults `BUILD_GRASS=False`) and tree density dialed to **0.10 ppsm (~1,966 trees)**. Landed ~50 FPS / 16 ms GPU. `stat fps` + resolution readout added to the range HUD. **R4 environment selectors:** two Time/Sky dropdowns on the R2 panel drive a new `AGolfRangeEnvironment` director actor (canonical UE pattern), find-or-spawned by HUD in PIE. `ApplyEnvironment()` composes Time (Dawn/Morning/Noon/Dusk/Night) × Sky (Clear/Cloudy/Overcast) → DirectionalLight rotation/temperature/intensity + fog + SkyLight intensity + VolumetricCloud `Cloud_GlobalCoverage`/`Cloud_GlobalDensity` (runtime MID). Pure C++, zero umap/asset changes — UE5.7 Basic level already ships the cloud + RealTimeCapture SkyLight + SkyAtmosphere. `golfsim.SetTime`/`golfsim.SetSky` console commands added.
- **Backlog moved to Linear (2026-05-25):** `docs/backlog.md` is gone; the prioritized work queue is now [linear.app/golfsim](https://linear.app/golfsim). Tickets are self-contained (goal + plan + done-when + files + pitfalls) so an agent can pick one cold. Filter by `machine/windows` or `machine/mac` to see what fits the current machine.
- **Active focus — OPEN-SOURCING (GolfForge), top priority:** all P0 publish-blockers DONE (GOL-43 license/AGPL, GOL-44 assets, GOL-45 secrets, GOL-46 attribution) + GOL-47 community files + GOL-60 portability + GOL-52/59/53 settings·credits·upscaler menu (TSR/DLSS/XeSS) + main menu + DPI-aware native-res render. **GOL-50 DONE** + **GOL-49 macOS half FUNCTIONAL** (working `.app` on Apple Silicon M4). **Identity hardening sequenced (GOL-76 → 77 → 78 → 79 → 80, plus GOL-81 anytime).** **Before the public flip:** GOL-76 domain + Workspace (Urgent — blocks the rest), then GOL-77 GH org transfer, then GOL-78-80 git identity work, then GOL-49 (committed `engine/scripts/package.sh` + Win64+macOS draft GitHub Release), GOL-51 (cross-platform pipeline). **Mac perf path:** GOL-72 (native 4K rendering on Mac, sibling of the Windows DPI fix; deferred to Low since fullscreen mode works) + GOL-64 (Mac/iPadOS MetalFX upscaling; blocked on engine MetalFX availability). The repo/module rename `golfsim`→GolfForge is folded into GOL-77.
- **Prior focus — feature track:** **EventBus (GOL-7) + manual-shot dialog (GOL-8) shipped; LM framework + OpenFlight (GOL-11) LIVE over Socket.IO** (GOL-36 fixed — `localhost`→IPv6 mismatch, not an lws bug; works against stock OpenFlight). A real launch monitor now drives the sim. Tracks from here:
  - **Ship target (this week):** range + a course (or a couple of holes) playable to OpenFlight users now that the LM is live.
  - **Square Omni driver (Medium, Windows):** **GOL-12** — second concrete `ULaunchMonitorDriver` on the proven framework (subclass, normalize its shot, `RegisterDriver` one line). What the pluggable seam was built for.
  - **Raw `/feed` upstream (Low, optional):** branch `feature/openflight-raw-feed` holds a simpler plain-WS transport; revisit if the OpenFlight maintainer takes the PR. Not needed for shipping.
  - **Sim correctness (Medium, Windows):** **GOL-9 ground roll DONE** (analytical pure-SI; carry → total + `final_lie`; panel Total row). Refinement now lives in **GOL-38** (multi-bounce/skip — the post-landing model is a flat slide today) + **GOL-39** (coefficient tuning, green spin-back, cross-surface, UDataAsset); **GOL-40** generalizes lie detection to real courses via splatmap PNGs.
  - **Pipeline polish (High, Mac/either):** **GOL-33** — GolfForge Demo's pipeline output has holes / missing tees / missing fairways. Audit + fix the OSM extraction before chasing more courses (GOL-19 second-course deferred until this lands). Windows can do the UE-side visual audit; the Python fix is Mac-side.
  - **Walking track (High, Mac):** **GOL-32** — order ESP32 + TCRT5000 parts (5-minute Amazon run). Unblocks **GOL-13** build → **GOL-14** FTMS Windows driver.
  - **Range polish (Low, Windows):** GOL-27 (R4 preset tuning), GOL-28 (yardage markers), GOL-29 (`SetPin` target), GOL-30 (Nanite backdrop), GOL-31 (static-Nanite trees) — all optional follow-ups from R3/R4.
  - **Course-quality arc (Medium, after GOL-33):** **GOL-34 bunker authoring tools** (raised lip + depression — sand traps that look like real bunkers), **GOL-35 fairway cut patterns** (stripes / criss-cross / diagonal mowing lines). Both blocked by GOL-33 — the polish pass establishes the corrected feature polygons these tools consume.
- History → `docs/worklog.md`. Next work → Linear. Engine gotchas → `docs/ue5-cookbook.md`.

## Session-end checklist

Before you stop a session, in order:

1. **Summarize** newly-completed work into `docs/worklog.md` (prepend a dated, tight entry — outcome + committed artifact, not blow-by-blow). Reference the Linear ticket ID (e.g., `GOL-5 R3 trees DONE`) so cross-links are easy.
2. **Update Linear:** mark shipped tickets Done, drop a brief outcome comment (numbers, what files landed). File new tickets for any follow-ups you discovered while working. Re-prioritize the top of the queue if your work changes what's next.
3. **Append** any new gotchas/recipes to `docs/ue5-cookbook.md`.
4. **Bump** the Current status section above (especially the "Latest" line and the active-focus pointer to specific Linear ticket IDs).
5. **Update** `docs/plan.md` if an architectural decision changed.
6. **Commit + push**, and tell the user what state the repo is in so the other machine knows what it'll see on `git pull`.

The next session — possibly on the other machine — starts by reading this file, scans Linear for the top-priority ticket tagged for its machine, and picks up from there.
