# Backlog — what's next (priority order)

> The prioritized work queue. When an item ships, summarize it into `docs/worklog.md` and drop it from here. Engine gotchas/recipes live in `docs/ue5-cookbook.md`.

1. **Practice-range POLISH pass (minimal level DONE 2026-05-23, see worklog).** The flat walkable+shootable range is built and is the default map; the user scoped the first session to "minimal testable" and these extras were deferred. All are **additive** — the splatmap already paints a perimeter Trees band and the material already binds Fairway grass, so no rework: **(a) 3D grass** — run existing `build_landscape_grass.py` (LGT_FairwayGrass already bound on `M_PracticeRange`), then USER triggers Build Grass Maps + stands near the fairway (camera-driven). **(b) Perimeter PCG trees** — the painted Trees band already gates placement (`build_pcg_treescatter.py` filters Trees>0.3, NO graph change needed); retarget `scatter_full_landscape.py`'s `LEVEL_HINT`+volume bounds to the ~504 m range (≈ ±25200 cm) and run its generate→report 2-call. **(c) Yardage markers** — new small script spawning `ATextRenderActor`s at 50–300 yd along +X from the PlayerStart tee (×91.44 cm/yd). **(d) `golfsim.SetPin <yards>` + `AGolfPinActor`** — chosen design: placeholder engine-basic-shape green/flag, distance measured from the PlayerStart tee along +X; **C++ editor-CLOSED rebuild** (new classes, like ball-flight). Add `SetPinCmd` to `GolfsimConsole.cpp` mirroring the `FAutoConsoleCommandWithWorldAndArgs`+`GetOrSpawnBall` pattern. **(e)** cozier lighting/colour — live knobs in `build_range_lighting.py` (`SUN_*`) and `build_range_material.py` (`LAYERS[*].tint`); the golden-hour read is currently fairly neutral. **(f)** USER-supplied far-end Nanite backdrop. Real LM driver stays the v0.3 milestone; shots are fired manually with `golfsim.FireShot` for now.

2. **Ball flight DONE (2026-05-22, see worklog).** Next sim-correctness pieces, in order: **(a) Ground interaction + roll (Chaos)** — turn the solver's carry/landing (position, speed, descent angle, lie) into `total_m` + `final_lie` via bounce/roll on the landing surface; this is the other half of `session.shot_outcome`. **(b) In-process EventBus** (`docs/event-protocol.md`) — the day-one architecture invariant still isn't built: stand up the `EventBus` subsystem, a `manual-shot-dialog` driver that publishes `shot.taken`, and a subscriber that runs `GolfBallFlight::Simulate` and emits `session.shot_outcome`. `FShotInput` already mirrors `shot.taken`, so this is mostly wiring + the pub/sub. **(c) Blueprint / live-tuning layer (additive, anytime — user requested 2026-05-22):** expose the solver for real-time tuning without recompiling. Promote `FShotInput` / `FAeroCoefficients` / `FBallTrajectory` to `USTRUCT(BlueprintType)` (UE5 takes `double` UPROPERTYs as Real pins), add a `UGolfBallFlightLibrary : UBlueprintFunctionLibrary` with `BlueprintCallable` `Simulate` / `TraceFromResolved`, and an `EditAnywhere FAeroCoefficients` on `AGolfBallActor` (or a `UDataAsset`) so Cd/Cl knobs are tweakable in the Details panel and the next shot uses them. Optional UMG slider panel (shot params + coefficients + Fire button) to dial the model against more reference data. Keep the pure-C++ integrator untouched — add only a thin wrapper/reflection layer so the headless tests + speed stay intact.

3. **M0.8.5 tree-scatter polish (deferred follow-ups, optional).** Scale-up is validated (29,070 Silver Birch @ 100 FPS / 4.3 GB on BethPage); these were explicitly punted at the stop-at-gate decision and can be picked up anytime: (a) tune `DENSITY_PPSM` / `TREES_THRESHOLD` in `build_pcg_treescatter.py` for a denser, more believable forest (0.02 was the de-risk/scale-up value, likely sparse); (b) add **Baltic Pine** (`Tree_Baltic_Pine`, already on disk — newer SkeletalMesh+ProceduralVegetationPreset format) as a 2nd species (2nd AddAttribute/Spawner branch or per-point random mesh attribute); (c) save `BethPageBlack.umap` with a (now non-transient) full volume so the forest persists across sessions — re-run `scatter_full_landscape.py` then save. Re-measure perf after density changes (29 k held 100 FPS / 4.3 GB; a much denser forest is the next stress point — `r.Streaming.PoolSize` is a fixed 1000 MB Scalability default, no over-budget at 29 k but watch `stat streaming` if density climbs). PCG Python API reference → `docs/ue5-cookbook.md`.

4. **ESP32 walking-pad driver (parallel hardware track).** Order LilyGo TTGO T-Display + TCRT5000 sensors. Optical-sensor mode with belt marks via 3D-printed jig.
