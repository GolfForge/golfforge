# UE5 / MCP / PCG cookbook

The engine-side knowledge base: hard-won pitfalls, working recipes, and the current Fab/Megascans asset list. Read on demand when building courses or working on the sim. **Append new gotchas/recipes here** as you hit them — `CLAUDE.md` links here and stays slim.

---

## Marketplace / Fab asset dependencies

Quixel/Fab/marketplace assets are NOT in git. Gitignored: `engine/**/Content/Megaplant_Library/`, `Megascans/`, `Fab/`, and `PC3D_Kentucky_Bluegrass_v14/` — re-downloadable free per machine. **UE5.7: Quixel Bridge is deprecated; Megascans now comes via the Fab plugin** (Window → Fab) and imports under `/Game/Fab/...`; some marketplace packs import to their own top-level `/Game/<Pack>/` (gitignore those explicitly). ("Asset not available in UAsset format" = use the Fab plugin's Add-to-project, not the old Bridge.) On a fresh clone, before opening `BethPageBlack.umap`, re-download the referenced assets via Fab or you'll see "missing asset" warnings.

**Currently referenced:** `Tree_Silver_Birch` (Megaplant, M0.8.5); Fab Megascans surfaces `Lawn_Grass_tkynejer` / `Uncut_Grass_oilpt20` / `Clover_Patches_on_Grass_sgmkajak` / `Bright_Desert_Sand_sjzkfega` / `Concrete_Floor_virrebs`; and **`PC3D_Kentucky_Bluegrass_v14`** (the fairway 3D grass — Fab/marketplace) (Material polish). `Thatching_Grass` / `Ribbon_Grass` / `Kikuyu_Grass` / `025_Grass` were trialed but the build references KBG. Add new dependencies here as milestones land.

---

## Course-import landscape path (UE5.7)

The primary all-Python landscape-import path via `unreal.LandscapeEditorObject.import_landscape_data()` is **dead in UE5.7** — `LandscapeEditorObject`, `LandscapeProxy.Import`, `LandscapeSubsystem`, and `WorldPartitionEditorSubsystem` are all unbound in the Python API. Use the manual fallback: human clicks Landscape mode → Manage tab → New Landscape → Import from File. Python automation handles everything else (material creation via `MaterialEditingLibrary`, WP cell loading via `WorldPartitionBlueprintLibrary.load_actors`, sphere spawn + physics setup, screenshots). For repeatable course imports later, either (a) write a stdlib-only PNG decoder + the Texture2D → RenderTarget → `landscape_import_heightmap_from_render_target` chain, or (b) escalate to a paid MCP that wraps the editor's import flow.

---

## PCG Python API reference

Verified across `build_pcg_treescatter.py` (graph) + `scatter_full_landscape.py` (volume/generate/perf) — those two scripts are the source of truth.

- `graph.add_node_of_type(SettingsClass) -> (PCGNode, PCGSettings)`; `graph.add_edge(from_node, Name(from_pin_label), to_node, Name(to_pin_label))`; `graph.remove_node`/`remove_edge`; `graph.get_input_node()`/`get_output_node()`; `graph.get_editor_property("nodes")`.
- Pin labels: `pin.get_editor_property("properties").get_editor_property("label")`; pins via `node.input_pins`/`node.output_pins` (properties, not methods). Input node OUT label=`In`; Output node IN label=`Out`; SurfaceSampler IN=`Surface`,`Bounding Shape`; AttributeFiltering kept-pin=`InsideFilter`.
- Modern class names: Create-Attribute = **`PCGAddAttributeSettings`** (PCGCreateAttributeSettings is deprecated); point filter = **`PCGAttributeFilteringSettings`** (PCGPointFilterSettings deprecated). Surface needs **`PCGGetLandscapeSettings`** (`sampling_properties.get_layer_weights=True`) on the SurfaceSampler `Surface` pin AND the graph **Input** node on the `Bounding Shape` pin — SurfaceSampler yields 0 points without a bounding shape.
- **Reusing the scatter graph for a 2nd map at a different density: give it its OWN graph.** Density lives on the SurfaceSampler node *inside the shared graph*. BethPage is validated at 0.02 ppsm; bumping the shared graph for the range (0.35) would mean a later BethPage scatter that fires *before* rebuilding the graph places ~17x the trees (perf disaster). `build_pcg_treescatter.py` now reads `GRAPH_PATH`/`DENSITY_PPSM` from `globals()` and seeds a missing sibling via `EditorAssetLibrary.duplicate_asset(BASE, GRAPH_PATH)`, so `DENSITY_PPSM=0.35; GRAPH_PATH="/Game/PCG/PCG_TreeScatter_Range"; exec(...)` authors a denser range graph in one call. `scatter_range_trees.py` is the range orchestrator — **persistent** (`generation_trigger=GenerateOnLoad`, save the umap, regenerates each load), the opposite of `scatter_full_landscape.py`'s transient measure-only gate.
- **InstancedSkinnedMeshComponent instance introspection (PCG SkinnedMeshSpawner output).** Count = `len(comp.get_editor_property("instance_data"))` — `get_instance_count()`/`get_num_instances()` silently return 0 for *skinned* ISM. Per-instance transform = `idata[i].get_editor_property("transform")` → a `Transform3f` whose `Vector3f` components are OPAQUE to Python (`repr` is `{}`, `to_tuple()` returns `()`, no `.x`); the reliable read is `t.export_text()` + regex on `Translation=(X=..,Y=..)`. `get_instance_transform(i, True)` also doesn't work on skinned ISM.
- **Live weightmap re-import regenerates PCG.** Re-importing a landscape's weightmaps (Landscape ▸ Manage ▸ Import, Heightmap unchecked) triggers an automatic PCG regen, so trees relocate to the new paint on import — re-running the orchestrator just confirms/forces it.
- **The GPU hogs are geometry/skinning, not pixels: 3D landscape grass + skinned PCG trees.** On the range, 4K fullscreen tanked to ~45 FPS. `stat unit` showed GPU-bound (36 ms vs ~13 ms game), and *lowering* `r.ScreenPercentage` made it **worse** — a tell that the cost is vertex/skinning, not fill (so TSR/DLSS upscaling won't save you). Diagnose by elimination: `grass.Enable 0` (live), toggle trees, watch the GPU ms delta. The dense camera-driven KBG `LandscapeGrass` on the fairway was ~half the GPU; trees the rest. Megaplant trees are **skeletal (skinned) meshes** — no static `SM_` variant ships — so each instance pays per-frame skinning regardless of resolution; many close + on-screen (a tight tree corridor) is expensive. Fixes that worked: remove the grass output from the range material, and keep tree density modest (range runs 0.10 ppsm ≈ ~1,900). A static-Nanite tree import would remove the skinning cost entirely.

---

## Known pitfalls / pending issues

- **`execute_unreal_python` sets `__file__` to a throwaway temp script, not the real script path.** It's `engine/Golfsim/Intermediate/UnrealClaudeMCPPython/exec_*.py`. Any `engine/scripts/*.py` that derives a repo path from `__file__` (e.g. `build_range_splatmap.py`'s `OUT_DIR = <root>/courses/...`) silently writes to the WRONG place (`engine/Golfsim/courses/...`) when exec'd in-editor vs run standalone. Guard: trust `__file__` only when `os.path.basename(dirname(__file__)) == "scripts"`, else hardcode the repo root.
- **Don't nuke+recreate a referenced material (e.g. a landscape material).** `build_*_material.py`'s backup→delete→recreate pattern fails on a re-run: `delete_asset` on a material the landscape references is blocked, so `create_asset` at the same path returns `None` (the script then `AttributeError`s) AND the failed delete **nulls the landscape's `landscape_material` → the terrain renders the default checker**. Recover by re-binding (`landscape.set_editor_property("landscape_material", mat)`). To *change* a live referenced material, edit it in place — e.g. to drop 3D grass, delete the `Landscape Grass Output` node in the Material Editor by hand (range-only; the course material keeps its grass). Note: you can't enumerate a material's expressions from Python in 5.7 (`editor_only_data` doesn't expose the collection, `Material.expressions` is protected, and `MaterialEditingLibrary` has only `get_num_material_expressions` / `delete_all` / delete-by-object — no get-all), so surgical node removal in script isn't available; the manual editor delete is the path.
- **Mac Cowork sandbox can't `unlink` files in `.git/`.** Symptom: `.git/index.lock` accumulates. Fix from host Mac terminal: `rm -f .git/index.lock`.
- **Mac Cowork sandbox is Linux, not macOS.** Don't create `.venv` from the Cowork bash — it'll have Linux-ELF binaries that the host Mac can't execute. Use `./setup.sh` from the host Mac terminal.
- **Windows Cowork sandbox can't `git clone` into the workspace mount.** Same family as the Mac note: writing into `.git/config` of a new clone fails with `Operation not permitted`. Do clones from PowerShell on the Windows side.
- **Sandbox `git status` shows LFS-tracked PNGs as "modified" on every fresh session.** The Linux Cowork sandbox doesn't ship `git-lfs`, so it can't run the smudge/clean filter and sees pointer-vs-binary diffs. Ignore in the sandbox; on Windows with LFS installed, `git status` is clean.
- **`.gitattributes` has a latent ordering bug.** `*  text=auto eol=lf` catch-all comes AFTER the `*.png ... -text` LFS lines, and git applies the LAST matching attribute, so `text=auto` wins on PNGs. Not breaking anything (LFS still works, content survives), but worth fixing: move the catch-all to the top of the file, or scope it to text extensions.
- **MSVC 14.51 (VS2026) is "not a preferred version" per UE5.7.4.** UE wants 14.44. Soft warning during build; the project compiled fine. If we hit weird linker errors on plugin builds later, this is the first suspect.
- **Cowork mode runs inside Claude Desktop.** Quitting Claude Desktop (e.g., to reload the MCP config) ends the active Cowork session. Always update `CLAUDE.md` and commit before restarting Claude Desktop so the next session picks up cleanly.
- **Overpass mirrors are flaky.** Script now retries across 4 mirrors. If all fail, just try again later — usually a few hours fixes it. (Observed: main `overpass-api.de` was 504ing all evening on May 13; `kumi.systems` recovered by morning.)
- **Argparse + negative longitudes:** use `--bbox-wgs84=-73.45,...` (equals sign), not `--bbox-wgs84 -73.45,...` (space), or argparse swallows the leading minus.
- **TestActor.cpp/h** were created during toolchain sanity check and aren't deleted yet. Harmless test scaffold (~600 bytes); delete via Content Browser → C++ Classes → Golfsim → TestActor → right-click → Delete next session, or leave.
- **`execute_unreal_python` does not return stdout via the bridge.** Tool result always shows `output: None`; actual `unreal.log()` output lands in the editor's Output Log. Use `get_log_lines(category_filter='LogPython', count=N)` to read it. For success/failure feedback, prefer `unreal.log(...)` followed by an explicit `get_log_lines` call rather than relying on the tool's return.
- **WorldPartition cells don't auto-load when the level opens via `save_current_level_as`.** Symptom: `inspect_landscape` reports `loaded_bounds = 0` and 0 streaming proxies even though the landscape exists. Fix: `unreal.WorldPartitionBlueprintLibrary.get_actor_descs()` → filter by `native_class.get_name()` containing 'Landscape' → `WPBL.load_actors([guid, ...])`. Loaded 256 of 320 proxies on first call; the rest load by camera proximity.
- **`MaterialEditingLibrary.connect_material_property` returns False silently** when the named output pin doesn't exist on the source expression. For `MaterialExpressionConstant3Vector`, the correct output name is `""` (empty string), not `"RGB"`. Same library has no `get_material_expressions`, and `Material.expressions` is protected from `get_editor_property`. Workaround: nuke + re-create via `MaterialFactoryNew` if a stale wire prevents reconnection.
- **Landscape PBR material authoring — hard-won (Material polish, `build_course_material.py` is the source of truth):** (1) **Landscape UVs MUST use `MaterialExpressionLandscapeLayerCoords`** (set `mapping_scale` > 0; default 0 = degenerate), NOT `MaterialExpressionTextureCoordinate`/TexCoord — on a landscape TexCoord samples near-uniform (texture reads as flat color, grid shows through, tiling changes do nothing). (2) **Quixel ORM packed textures must sample as `SAMPLERTYPE_MASKS`** not LinearColor or the *whole material* fails to compile → landscape renders the default grey checker ("Sampler type is Linear Color, should be Masks"). ORM channels = R:occlusion **G:roughness** B:metallic. (3) **Many textured layers exceed the 16-sampler cap** — set every `TextureSample.sampler_source = unreal.SamplerSourceMode.SSM_WRAP_WORLD_GROUP_SETTINGS` (shared) so 7 layers ×3 textures compile. (4) **Nuke+recreate of a landscape material at the same path sets the Landscape actor's `landscape_material` to None** (the soft-path-rebinds assumption is false) → the terrain renders the checker until reassigned; the script must `landscape_actor.set_editor_property("landscape_material", mat)` after rebuild (and the user re-adds Paint Target Layers via "Add From Material(s)" if they drop — though here the LIIs persisted). (5) Macro-variation (lerp detail albedo with a far-larger-scale sample of itself) is needed to kill tiling repeat on flyover/ball-cam. (6) `connect_material_expressions(src, out_pin, dst, in_pin)` returns False *silently* on a bad pin — log every call. (7) UE landscape **grass is camera-driven**; `LandscapeSubsystem` is unbound, no Python force-build — `LandscapeGrassType` instances only spawn near the viewport within cull distance after grass maps lazily build.
- **Removing a plugin after it touched/auto-spawned actors in a level makes the `.umap` UNLOADABLE — invisible in the Content Browser, not just a warning.** Symptom: a valid, committed `.umap` (correct package magic `c1 83 2a 9e`, full size, not an LFS pointer) does not appear in the Content Browser even via search; `EditorAssetLibrary.does_asset_exist` is False even after `AssetRegistry.scan_paths_synchronous(..., force_rescan=True)`; the log shows **`LogAssetRegistry: ... uses an unknown custom version and cannot be loaded for the AssetRegistry`**. Cause: the package was saved while a plugin was enabled (here the **Water** plugin during M0.9 — it auto-spawned a `WaterBrushManager` + WaterBodyTexture render targets that were never deleted, baking `/Script/WaterEditor` refs + the plugin's FCustomVersion into the package); the plugin was later removed from `.uproject`, so that custom version is now "unknown" and the AssetRegistry refuses the package. **Diagnose:** `grep -aoE "/Script/[A-Za-z0-9_]+" the.umap | sort -u` — any `/Script/<Plugin>` whose module isn't currently loaded is the culprit (by elimination: the only plugin you *removed* this session). **Fix:** re-enable that plugin in `Golfsim.uproject` (+ re-add any ini it needs, e.g. the `WaterBodyCollision` `[/Script/Engine.CollisionProfile]` profile) and restart — the version becomes known, the map loads, ZERO data loss. **Lesson:** before removing a plugin, delete *all* actors/objects it spawned (incl. auto-spawned managers like `WaterBrushManager`) and re-save the level so its custom version is dropped from the package; otherwise the plugin is a permanent umap dependency. Autosaves share the contaminated header so they don't help; git history is fine (the file was never corrupt).
- **StaticMeshActor mobility defaults to Static.** `set_simulate_physics(True)` silently does nothing on a Static component — no warning, no error. Always call `smc.set_mobility(unreal.ComponentMobility.MOVABLE)` before enabling physics, then `set_collision_profile_name('BlockAll')` to actually collide with the landscape.
- **Open World Template default landscape leaves WP HLOD orphans.** Deleting the `Landscape` actor via `delete_actor` removes only the parent; 64 `LandscapeStreamingProxy` actors and 64 `WorldPartitionHLOD` actors remain. Bulk-delete via `EditorActorSubsystem.destroy_actor()` in a Python loop. For new courses, **always** start from a Basic empty level instead of Open World Template (next bullet for why).
- **"Save Level As" off the unsaved Open World Template creates a level INSTANCE, not a copy. PIE/Simulate then crashes with `Assertion failed: Actor->GetLevel() == DestLevel`.** First M0 attempt saved Untitled_1 (Open World Template) as `/Game/Maps/BethPageBlack`. The new landscape's streaming proxies got created with packages under `/Engine/__ExternalActors__/Maps/Templates/OpenWorld/...` (the template's path) instead of under our map's path. When PIE tries to instantiate the level, the level paths don't match → fatal assertion. **Fix**: import courses into a brand-new **File → New Level → Basic** (not Open World, not Empty Open World — both have WP). Basic levels in this project don't enable World Partition; the entire landscape lives inline in the .umap, no external actors at all.
- **`unreal.AutomationLibrary.take_high_res_screenshot` and `HighResShot` console command silently no-op when UE editor is backgrounded.** Symptom: dispatch returns success, but no PNG ever lands on disk. UE pauses viewport rendering when the editor window doesn't have focus (resource-saving). For programmatic screenshots that work without focus, use the bridge's `get_viewport_screenshot` (synchronous, returns inline base64; decode the saved tool-result via `ConvertFrom-Json` + `[Convert]::FromBase64String`). It returns the last-rendered frame, which may itself be stale if the viewport hasn't redrawn since the last user interaction — moving the camera via `set_camera_transform` doesn't always trigger a re-render either.
- **MCP edits are in editor-memory only until something saves.** `spawn_actor`, `set_actor_property`, `delete_actor`, and any `execute_unreal_python` mutation dirty UObjects but do not write `.umap` / `.uasset` files. Symptom: next UE session opens to a "wait, where did my sphere/lighting/material go?" state. After any meaningful MCP edit chain, call `save_dirty_assets` (the bridge tool wraps UE's "Save All"). Cheap and idempotent; just always call it at the end of an MCP-driven sequence before the user closes UE or before you check `git status` expecting to see new files.
- **`LandscapeLayerInfoObject` is half-bound in 5.7 Python: createable but barely configurable.** `LandscapeLayerInfoObjectFactory` is unbound (no `AssetTools.create_asset` path). `unreal.new_object` works but creates the object in `/Engine/Transient` — useless without a real package. Even when you wire up a real package, only `hardness` and `phys_material` are settable; `layer_name` is read-only and `bNoWeightBlend` (the weight-blend toggle) is not exposed at all. Path: have the user create the 4 LayerInfos via Landscape > Paint mode's `+` button (UE auto-sets `LayerName` to match the material's `LandscapeLayerWeight` parameter), then double-click each LII asset and switch from "Non Weight Blended" to **Legacy Weight Blending** in the Details panel. Python is fine for the material side; the LayerInfo side is a manual handoff.
- **UE5.7 Landscape Paint UI doesn't auto-populate Target Layers from a freshly-assigned material.** Symptom: you `set_actor_property` `LandscapeMaterial` to a new layer-blend material, switch to Paint tab, see "There are currently no target layers assigned to this landscape." `Landscape.force_layers_full_update()` and `get_target_layer_names()` don't help (former is for Edit Layers, latter returns []). Fix: small "Add From Material(s)" button in the Target Layers section toolbar of the Paint panel — one click and the layers appear. Not Python-driveable that we found.
- **UE5.7 Landscape > Manage > Import dialog's "Layers" array is the right place to bulk-import per-layer weight PNGs.** Right-click on a layer in the Paint panel doesn't have an Import option in 5.7 (UI moved). In Manage tab > Import: uncheck "Heightmap File" (so existing elevation isn't overwritten), expand the "Layers" array, set the path on each layer row to the matching `splat_<name>.png`, click Import. Single round-trip for all layers.
- **Quixel Megaplant assets ship in two incompatible shapes — same library, different formats per pack.** Older Megaplant trees (e.g., Silver Birch) ship as plain `StaticMesh` files under `<TreeName>/Instances/<TreeName>_Large_A.uasset` etc. — drop straight into a standard `FoliageType_InstancedStaticMesh`. Newer Megaplant trees (e.g., Baltic Pine) ship as **SkeletalMesh** + a sibling `ProceduralVegetationPreset` data asset under `<TreeName>/<TreeName>_01/<TreeName>_01_A.uasset`, plus a separate `Instances/` folder of skinned branch sub-pieces. The newer format is meant to be consumed by PCG using the procedural preset, NOT bolted into a Foliage Type. Convert-to-StaticMesh is technically possible but throws away the wind animation + variation system that's the whole point of the new format. **Action when you hit this**: prefer the asset that ships as StaticMesh; defer SkeletalMesh+ProceduralVegetation assets to a PCG-based milestone (we did this for Baltic Pine — see backlog M0.8.5).
- **UE Foliage `density` unit is "instances per 1000² unreal units" = 100 m².** Easy to be off by 10x because "1000²" reads like 1000 m² when it's actually 100 m². Density 100 = 1 tree per m² (jungle); density 8 ≈ 1 tree per 12.5 m² (sparse mature forest); density 1 = 1 per 100 m² (orchard). Hit this: set density 150 expecting "1 per 6.7 m²", actually got 1.1M instances on Bethpage's wooded ~30%. For golf-course perimeter forest, **density 5-10** is the right zone; tune by erase-and-repaint after observing the count.
- **`FoliageType_InstancedStaticMeshFactory` IS Python-bound in 5.7** (unlike `LandscapeLayerInfoObjectFactory`). Foliage Type properties — `mesh`, `density`, `random_yaw`, `random_pitch_angle`, `scale_x/y/z` (each a `FloatInterval` struct), `landscape_layers` (TArray<FName>), `minimum_layer_weight` (float), `ground_slope_angle` (FloatInterval), `align_to_normal`, `align_max_angle`, `z_offset`, `collision_with_world` — are all settable via `set_editor_property`. The full Foliage Type setup script is reusable verbatim across courses.
- **Foliage Mode "Fill" tool can silently no-op** depending on landscape selection / build quirks. Symptom: click Fill on the landscape, nothing spawns, no error. Reliable fallback: switch to **Paint** with maximum brush size and drag-sweep across the landscape — the per-Foliage-Type filters (landscape layer, slope, etc.) still gate placement, so painting "everywhere" is fine; it's not actually painting trees on bunkers.
- **Foliage Mode Reapply tool's "Density Scaling" checkbox is a per-instance render multiplier, not a count adjustment.** Symptom: change Foliage Type density 150 → 8, enable Reapply > Density Scaling, brush over instances — count doesn't change. Density Scaling is the per-instance equivalent of the global `foliage.DensityScale` CVar (used for runtime perf tuning). To actually change instance count after density edit: right-click the Foliage Type in the palette → "**Remove this Foliage Type from the palette, removes all associated instances as well**" → drag the asset back in → Paint at max brush. Clean nuke + repaint is the reliable workflow.
- **Megaplant trees rendering GREY up close but textured at distance = the `ProceduralVegetationEditor` Experimental plugin is disabled.** Megaplant tree material instances (`MI_<Tree>_01_Bark` / `_Bark_Blend` / `_Foliage`) parent off master materials that live *inside* the `ProceduralVegetationEditor` plugin (`/ProceduralVegetationEditor/SampleAssets/Materials/MasterMaterials/MA_Foliage_Trees*`), along with `PVE_<Tree>_01` (the ProceduralVegetationGraph) and `DefaultTreeWindSettings`. If the plugin isn't enabled, those packages can't mount, the MIs lose their parent, and they fall back to the default grey material. Tell-tale log lines (read via `get_log_lines(min_verbosity='Warning')`): `Skipped package /ProceduralVegetationEditor/... plugin Procedural Vegetation Editor ... not mounted [Enabled = FALSE]`. The symptom is *inverted-LOD* — fine at distance (the asset's self-contained imposter LOD), grey up close (the real SkeletalMesh + broken-parent MIs swap in) — which reads exactly backwards from how LOD/Nanite should behave. `get_material()` still reports the slots "correctly assigned" because the MI *references* are intact; only their parent fails at render time, so asset inspection won't catch this — only the viewport / the log does. **Fix: add `{ "Name": "ProceduralVegetationEditor", "Enabled": true }` to `engine/Golfsim/Golfsim.uproject` Plugins (it auto-enables its Dataflow/GeometryScripting/DynamicWind deps; PCG already on), restart the editor, regenerate.** Plugin ships prebuilt engine binaries (`D:/ue57/UE_5.7/Engine/Plugins/Experimental/ProceduralVegetationEditor`) so no C++ rebuild. This was found 2026-05-16 and is now committed in the .uproject — but flag it for any *other* machine / fresh clone that opens the project.
- **THE MEGAPLANT SAGA — Quixel Megaplant trees cannot be mass-scattered through legacy Foliage in UE5.7. Don't retry these paths; go straight to PCG.** Modern Quixel Megaplant tree packs (e.g., Tree_Silver_Birch, Tree_Baltic_Pine) ship as: (a) **Nanite SkeletalMesh** full trees at `<Tree>/Tree_<Name>_01/SK_<Name>_01_A..D` + a `ProceduralVegetationPreset` (`PVE_*_Data`) + `PVWindSettings`; (b) a misleading `Instances/` folder of **StaticMesh scatter sub-pieces** (`<Name>_Large_A..H` ≈ 1.16 m leaf-cluster blobs, NOT trees — "Large" = largest *scatter part*). Every legacy-Foliage path was tried and failed: **(1)** `FoliageType_InstancedStaticMesh` pointed at `Instances/*_Large_A` → spawns 1.16 m shrubs, not trees. **(2)** SkeletalMesh → StaticMesh convert → the one-click action **was removed in UE5.7** (gone from Content Browser + actor right-click). **(3)** Merge Actors bake → fails "insufficient mesh components" — it only bakes StaticMeshComponents, never skeletal. **(4)** `FoliageType_Actor` wrapping the SkeletalMesh in a BP → works functionally but Megaplant skeletal is ~2 MB VRAM/instance (Nanite skeletal + 4K bark/foliage textures, no instance dedup, no auto-imposters), so ~3K trees = 6.4 GB VRAM → editor over-budget + crash. **Root insight**: these assets are designed for Quixel's procedural pipeline (PCG consuming the `ProceduralVegetationPreset`), which auto-generates distance imposters/billboards — that LOD machinery is the *only* thing that makes a forest of them memory-viable. The painted `Trees` landscape layer (LII_Trees + weights + material node) survives as the M0.8 deliverable and feeds M0.8.5's PCG pass. **For future course imports: do not attempt Megaplant-via-Foliage. Either use the PCG path, or pick genuinely lightweight StaticMesh trees for mass background scatter and reserve Megaplant for hand-placed hero trees.**
- **PCG attribute selectors + SoftObjectPath: the Python typed setters are silent no-ops; only `import_text` with the fully-wrapped form works.** `PCGAttributePropertyInputSelector` / `PCGAttributePropertyOutputSelector` (used by `AddAttribute.output_target`, `AttributeFiltering.target_attribute`, `SkinnedMeshSelector.mesh_attribute`): `set_attribute_name(...)` and bare `import_text("Name")` do NOTHING — the selector stays at its `@Last` default (and `@Last` does NOT auto-resolve to an upstream-created attribute at PCG generation time: you get `Mesh Attribute '@Last' is not in the metadata`). The ONLY form that takes is `selector.import_text("PCGBegin(<AttrName>)PCGEnd")` then set the struct back on its owner; verified persistent across save + reload. Same family: `unreal.SoftObjectPath("/Game/...")` constructs **empty** (ctor ignores the arg, struct has no settable props) — populate via `sopv = const.get_editor_property("soft_object_path_value"); sopv.import_text(path); const.set_editor_property("soft_object_path_value", sopv); settings.set_editor_property("attribute_types", const)`. `PCGSkinnedMeshSpawnerSettings.mesh_selector_parameters` is a read-only UObject ref — mutate its `mesh_attribute` in place, never set the param back. All of this is encoded in `engine/scripts/build_pcg_treescatter.py`; reuse it, don't re-derive.
- **PCG SurfaceSampler needs BOTH a Surface and a Bounding Shape, and `PCGVolume` spawned via Python defaults tiny + must be tall enough to span the terrain.** Surface Sampler with `Surface`=`PCGGetLandscapeSettings` but no `Bounding Shape` → 0 points (output is passthrough `PCGVolumeData`, not points). Wire the graph **Input** node to `Bounding Shape` (it supplies the PCG component/volume bounds). A `PCGVolume` from `spawn_actor_from_class` has a default brush half-extent of **100 cm** (not 2500 — `get_actor_bounds` lies for volumes; trust `unreal.PCGBlueprintHelpers.get_actor_bounds_pcg(vol, True)`), so a 500 m test box needs `set_actor_scale3d((250,250,40))`. Critically the Z half-extent must exceed the local terrain relief at the volume's location or SurfaceSampler finds no surface inside the box and yields 0 points (this, plus the tiny default, caused a long false-negative chase — trees only appeared after Z half ≈ 4000 cm). `component.generate(True)` is **async**: read instance counts / `instance_data` length in a *later* tool call, not the same one. The throwaway test volume's scale does NOT persist unless `BethPageBlack.umap` is saved (it reverts to the level-saved scale on editor restart).
- **Counting PCG-spawned skinned instances: use the `instance_data` array length, NOT `get_instance_count()`.** PCG `SkinnedMeshSpawner` output is an `InstancedSkinnedMeshComponent`. Its `get_instance_count()` / `get_num_instances()` exist but **silently return 0** (no exception) — those getters are `InstancedStaticMeshComponent`-only. The real count is `len(comp.get_editor_property("instance_data"))` (an Array; verified 29070 at full-course scale). `instance_count` / `num_instances` / `per_instance_s_m_data` properties do NOT exist on the skinned class. Also: the spawned components live on the `PCGVolume` actor itself (alongside its `BrushComponent`), found via `actor.get_components_by_class(unreal.InstancedSkinnedMeshComponent)` — not under a separate PCG-managed/partition actor for a non-partitioned volume. Encoded in `engine/scripts/scatter_full_landscape.py::_instance_count`. Open script-polish nit: `comp.get_instance_transform(i, True)` did not yield transforms for the skinned component (the spatial-spread sampler no-ops); rely on the user's visual map-wide confirmation, or fix the skinned `get_instance_transform` signature later — not gate-blocking.
- **UE Water plugin is NOT scriptable for rendering in 5.7 — use flat DynamicMesh water instead (the golf-sim-standard approach).** `WaterBodyLake`/`WaterZone` spawn + spline-seed fine from Python, but the water surface only renders after `AWaterZone::MarkForRebuild()`, which is C++/editor-only and **not Python-bound** (`hasattr(zone,'mark_for_rebuild')` is False; no `WaterZoneRebuildFlags`; `WaterSubsystem` is a world- not editor-subsystem; manual gizmo-nudge per body works but is inconsistent). Also the plugin auto-creates a `WaterZone` on first body add (creating your own makes a redundant second one that breaks rendering), needs the `Water` plugin + a `WaterBodyCollision` DefaultEngine.ini profile + an editor restart. Abandoned for `golfsim` M0.9. **Working recipe (`engine/scripts/build_water_actors.py`, the source of truth):** `DynamicMeshActor` (spawnable; bare `Actor` has NO `add_component_by_class` in 5.7 so ProceduralMeshComponent can't be bolted on) → `actor.dynamic_mesh_component` → `get_dynamic_mesh()` → `unreal.GeometryScript_Primitives.append_triangulated_polygon(mesh, GeometryScriptPrimitiveOptions(), Transform(), [Vector2D...], True)` for a flat tri-meshed polygon → `dmc.set_material(0, mat)` → collision via `unreal.GeometryScript_Collision.set_dynamic_mesh_collision_from_mesh(mesh, dmc, GeometryScriptCollisionFromMeshOptions())` + `set_collision_profile_name`/`set_collision_enabled`. GeometryScript Python classes are `unreal.GeometryScript_Primitives` / `_Collision` / `_Normals` / `_PolygonList` etc. (NOT `GeometryScript_MeshPrimitiveFunctions`). Renders immediately, no plugin, no restart, idempotent.
- **`SystemLibrary.line_trace_single` for landscape height: must be COMPLEX, and HitResult is read via `.to_tuple()`.** `trace_complex=False` misses the landscape heightfield entirely (returns empty). `HitResult` fields are NOT attribute- or `get_editor_property`-readable (`blocking_hit`/`impact_point` raise; `Location`/`Distance` are protected). Use `hit.to_tuple()`: index 0 = `bBlockingHit` (bool), index 5 = `ImpactPoint` (Vector). An off-landscape XY returns `None` (clean miss, not an empty HitResult). Encoded in `build_water_actors.py::_ground_z`. Trace span far wider than the ~+/-2150 cm relief is free insurance.
- **Editor FPS can't be read from Python and the screenshot tool is doubly unreliable for perf.** No `SystemLibrary.get_average_fps`. Sampling `get_frame_count` over wall-clock gives garbage (~3 FPS) because UE throttles the editor to a few FPS when its window is backgrounded — the same reason `get_viewport_screenshot` returns a stale/empty frame (it showed bare landscape while 2265 trees were actually rendering, confirmed by the user looking at their focused viewport). For a real perf number: enable `stat fps`/`stat unit` via `SystemLibrary.execute_console_command(world, "stat unit")` and have the **user read the focused viewport** — that's the only trustworthy path here.
- **Creating a Blueprint actor with a component via Python works via `SubobjectDataSubsystem`** (used for `BP_SilverBirch`): `BlueprintFactory` with `parent_class=Actor` → `create_asset` → `SubobjectDataSubsystem.k2_gather_subobject_data_for_blueprint(bp)` for the root handle → `add_new_subobject(AddNewSubobjectParams(parent_handle, new_class=..., blueprint_context=bp))` → `rename_subobject` → set props on the template via `SubobjectDataBlueprintFunctionLibrary.get_object(data)` (deprecated but works; `get_associated_object` is the non-deprecated replacement) → `BlueprintEditorLibrary.compile_blueprint` → save. `FoliageType_ActorFactory` and `FoliageType_InstancedStaticMeshFactory` are both Python-bound; `FoliageType_Actor.actor_class` takes a `BlueprintEditorLibrary.generated_class(bp)` result. The recipe is sound even though we're abandoning the asset it built.
- **Range-only "fixed on the tee" aim mode without touching shared input assets (range R1, 2026-05-23).** The range and BethPage share `BP_FirstPersonGameMode` + `IMC_Default`, so editing the IMC to kill WASD/mouse-look would disable walking on the *course* too. Instead, lock input range-only from the level's own `AHUD` (`AGolfRangeHUD`, which only the range uses): once the PlayerController exists, `PC->SetIgnoreMoveInput(true)` + `PC->SetIgnoreLookInput(true)` — engine-level ignore counters that gate `AddMovementInput` and `AddControllerYaw/PitchInput`, so the FP template's Enhanced-Input Move/Look bindings no-op **without** removing the IMC or editing the BP graph. Drive aim by binding Left/Right on the HUD's legacy `InputComponent` (arrows aren't in `IMC_Default`, so no Enhanced-Input conflict) and, in `Tick`, `PC->SetControlRotation(yaw += rate*dt)`. Key subtlety: `SetControlRotation` is **NOT** gated by `SetIgnoreLookInput` (only `AddYaw/PitchInput` is), so it still turns the FP camera (which follows control rotation — that's how mouse-look worked in the first place). Read `PC->GetControlRotation()` for the shot heading so view and shot always match. All in `GolfRangeHUD.h/.cpp`; no shared-asset edits, BethPage unaffected. (`AHUD` needs `PrimaryActorTick.bCanEverTick = true` in its constructor to get `Tick`.)
- **UE5.7 Python asset-edit gotchas (range spacebar tool, 2026-05-23).** (1) **Editing a Blueprint's CDO default via Python** (`unreal.get_default_object(unreal.EditorAssetLibrary.load_blueprint_class(path))` → `set_editor_property(...)`) reads back correct and persists on `save_asset`, but does NOT take effect in the already-running editor / next PIE until you `unreal.BlueprintEditorLibrary.compile_blueprint(bp)` (compile preserves the CDO value via copy-to-new-CDO). Symptom hit: a GameMode HUDClass set this way produced NO HUD in PIE until the BP was recompiled. (2) **`UInputMappingContext.mappings` is deprecated and reads EMPTY in 5.7** — editing it is a silent no-op (set returns success, nothing changes). The real key bindings live in the **`default_key_mappings`** struct (`InputMappingContextMappingData`), whose `.mappings` array holds `FEnhancedActionKeyMapping` entries (`action`=UInputAction, `key`=FKey, key name via `key.get_editor_property("key_name")`). Recipe: get the struct, filter its `mappings`, set the array back on the struct, set the struct back on `default_key_mappings`, `save_asset`. Used to strip Space→IA_Jump (kept the gamepad-jump entry). (3) Minor name traps: it's `unreal.EditorAssetLibrary.load_blueprint_class` (there is NO `unreal.load_blueprint_class`); `PlayerController.get_hud()` exists but `get_pawn` does not (use `get_controlled_pawn`); `WorldSettings` is absent from `EditorActorSubsystem.get_all_level_actors()` (get it via `UnrealEditorSubsystem.get_editor_world().get_world_settings()` or `GameplayStatics.get_all_actors_of_class`).
- **Pure-C++ UMG widget — no WBP asset (range R2, 2026-05-23, `GolfRangePanel.h/.cpp` is the source of truth).** A `UUserWidget` subclass can build its entire tree in C++ with zero Blueprint asset — fits this repo's all-source ethos and dodges the MCP/Python widget-authoring fragility. Recipe + the traps that cost cycles: **(1) Build the tree in `NativeOnInitialized()`** (not `RebuildWidget()` — that's for primitive `UWidget` subclasses; overriding it on a UserWidget bypasses the machinery). `WidgetTree` is valid there because the HUD creates the widget with a PlayerController (`CreateWidget<T>(PC, T::StaticClass())`). **You MUST set `WidgetTree->RootWidget = <root>`** or the base `RebuildWidget` renders an empty `SSpacer` (silent blank widget). Construct children with `WidgetTree->ConstructWidget<UType>()` (it's `[[nodiscard]]`); hold refs in `UPROPERTY(Transient) TObjectPtr<>` members so GC keeps them. **(2) Top-right placement under `AddToViewport`** (which fills the screen): root = `UCanvasPanel`, add a `UBorder`, on its `UCanvasPanelSlot` set `SetAnchors(FAnchors(1,0,1,0))` + `SetAlignment(FVector2D(1,0))` + `SetAutoSize(true)` + `SetOffsets(FMargin(0,T,R,0))`. **(3) `UTextBlock` default font needs no asset** — `Font = GetFont()` is engine Roboto already; mutate `FSlateFontInfo F = GetFont(); F.Size=N; SetFont(F)`. **(4) `UComboBoxString` renders fully styled code-only** in 5.7 (ctor defaults WidgetStyle/ItemStyle/Font from the style cache) — just `AddOption(...)`. **(5) ComboBox selection feedback loop:** `SetSelectedIndex()` re-broadcasts `OnSelectionChanged` with `ESelectInfo::Direct`, so syncing the dropdown from code (e.g. 1-6 keys → dropdown) loops back into your handler. Guard: bind a `UFUNCTION() void(FString, ESelectInfo::Type)` and early-return on `SelectionType == ESelectInfo::Direct` (plus a `bool` reentrancy flag around your `SetSelectedIndex` call). Module deps: add `"UMG", "Slate", "SlateCore"` to `PublicDependencyModuleNames` (Public, since the widget header is included by the HUD).
- **UMG widget color knobs that bit us (range R2).** `UComboBoxString::InitForegroundColor()` is **protected** — you cannot set the closed-button text color from outside; direct `ForegroundColor` access is deprecated + construction-only. For the **open dropdown rows**, the default `ItemStyle.TextColor` is **dark** (unreadable on a dark panel) — fix via the public `FTableRowStyle s = combo->GetItemStyle(); s.TextColor = FSlateColor(FLinearColor::White); combo->SetItemStyle(s);` (leave `SelectedTextColor` dark so the hovered row reads on its light highlight). `#include "Styling/SlateTypes.h"` for `FTableRowStyle`.
- **Clicking a UMG widget in a first-person PIE session — two gotchas (range R2).** (1) **The game viewport captures/hides the mouse**, so a UMG ComboBox/button is unclickable by default. Free the cursor *range-only* from the controller (alongside the R1 input lock): `PC->bShowMouseCursor = true;` + `FInputModeGameAndUI m; m.SetLockMouseToViewportBehavior(EMouseLockMode::DoNotLock); m.SetHideCursorDuringCapture(false); PC->SetInputMode(m);` (`FInputModeGameAndUI`/`EMouseLockMode` are in `GameFramework/PlayerController.h`). GameAndUI keeps legacy key binds (1-6/Space/arrows) live while routing clicks to the UI. (2) **A clicked ComboBox keeps keyboard focus**, so Space (Slate's "activate" key) toggles the dropdown instead of reaching your `SpaceBar`→fire bind. Fix: after a genuine pick, return focus to the game — `FSlateApplication::Get().SetAllUserFocusToGameViewport()` — **deferred one tick** (`World->GetTimerManager().SetTimerForNextTick([]{...})`) so it runs after the combo's own post-selection focus handling. Edge case left as-is: re-picking the *same* option doesn't fire `OnSelectionChanged`, so focus isn't returned (uncommon).
- **Live Coding vs editor-closed rebuild (when each works, range R2).** Body-only `.cpp` changes — including adding `#include`s — patch cleanly via Live Coding (Ctrl+Alt+F11) without closing the editor: ~3 s, `[n/n] Compile … → Patch creation … successful`. What needs an **editor-closed full rebuild** instead: a new `UCLASS`, a new module dependency in `.Build.cs`, or **a new `UPROPERTY`/`UFUNCTION`** (reflection/layout change — UHT must regen). Note when iterating: a fix in a per-event callback (e.g. `OnSelectionChanged`) takes effect in the *running* PIE after the patch, but a fix in `NativeOnInitialized`/`BuildTree` only runs on a *fresh* widget — restart PIE to see it. A failed Live Coding compile leaves the prior patch in place (no partial apply), and a Live-Coding re-instanced session can transiently mis-render styles (we briefly saw "default" colors that weren't) — trust a fresh PIE, not a re-instanced one.
- **Custom hardware cursor in UE5.7 (range cursor, 2026-05-23).** A hardware cursor loads from a **raw image file under `Content/`, not a Texture2D `.uasset`**. Set it at runtime: `World->GetGameViewport()->SetHardwareCursor(EMouseCursor::Default, FName(TEXT("Slate/golf_tee_cursor")), FVector2D(hx,hy))`. Path resolution (verified GameViewportClient.cpp:4611): `FPaths::ProjectContentDir() / GameContentPath` + extension search (`.ani`/`.cur`/`.png`, plus DPI-suffixed `name24.png` etc.), so `"Slate/golf_tee_cursor"` → `Content/Slate/golf_tee_cursor.png`. **HotSpot is normalized 0–1** (`xHotspot = HotSpot.X * Width`, WindowsCursor.cpp:218), so it's resolution-independent — you can swap the PNG for a different size without touching code. Color cursors are built via `CreateIconIndirect` at the PNG's authored size (no hard 32px clamp; 64px works fine; on Windows the cursor is a fixed N screen-px regardless of the PIE/window size, so it looks proportionally large in a small windowed PIE and correct at fullscreen). `SetHardwareCursor` is on `UGameViewportClient` (public, ENGINE_API) — `APlayerController` has no direct one; reach it via `GetWorld()->GetGameViewport()`. Override **both `EMouseCursor::Default` and `::Hand`** so the cursor persists when hovering UMG (combobox/buttons may request Hand). Includes: `Engine/GameViewportClient.h` + `GenericPlatform/ICursor.h`. We set it range-only in `AGolfRangeHUD`'s controls-lock block (next to `bShowMouseCursor`/`FInputModeGameAndUI`); it's a body-only change so Live-Coding-able, but the once-per-session block means a fresh PIE is needed to see it.
- **Matting a white background off glow art (offline, .NET System.Drawing — no Photoshop/ImageMagick/PIL needed on Windows).** For art on a solid white bg (e.g. a cursor), a naive `-transparent white` color-key eats interior white highlights and leaves a hard halo where a glow fades into white. What worked (one-off via PowerShell `Add-Type` C#, `[System.Drawing.Bitmap]` + `LockBits`/`Marshal.Copy`): (1) **flood-fill the connected near-white from the image edges** (`min(R,G,B) >= ~248`) → alpha 0; this only removes the background, preserving enclosed interior highlights; (2) for the rest, **ramp alpha by brightness** `a = clamp((rampHi - min(R,G,B))/(rampHi - rampLo))` (≈205→255) so the warm glow falloff becomes a *semi-transparent* halo instead of an opaque pale ring — reads correctly on any background (a neutral-white bg vs a warm glow separate cleanly because the glow has low blue, i.e. low `min`). Then **crop to the alpha bbox, premultiplied-downscale** (draw through a `Format32bppPArgb` surface so edges don't fringe white), and rotate as needed. Compositing the result over dark + the actual scene color (grass green) side-by-side is the way to judge halo/fringe — over white you can't see the alpha at all.
- **The UE5.7 Basic level already ships a full dynamic sky kit — incl. a VolumetricCloud and a RealTimeCapture SkyLight (range R4, 2026-05-23).** A File>New Level>Basic map comes with `DirectionalLight` (atmosphere sun), `SkyAtmosphere`, `ExponentialHeightFog`, a `SkyLight` **already set Movable + `bRealTimeCapture=true`**, AND an `AVolumetricCloud` (using `m_SimpleVolumetricCloud_Inst`) — exactly one of each. Verify with `EditorActorSubsystem.get_all_level_actors()` + a class-name count before assuming you need to spawn anything. Two big consequences for runtime time-of-day/weather: (1) **no manual SkyLight recapture** — with `bRealTimeCapture` on, ambient auto-tracks the moving sun every frame, so changing the DirectionalLight rotation/intensity at runtime updates ambient for free (don't call `RecaptureSky()`); (2) the cloud is **already in the GPU budget**, so making coverage/density controllable adds ~nothing (on the range, 4K/~16 ms held; heavier overcast was even slightly *cheaper* because its sun is dimmer). `build_range_lighting.py` predates this and only tuned sun/fog/skylight; R4 added cloud control without spawning anything.
- **VolumetricCloud coverage/density at runtime = drive the cloud material's `Cloud_GlobalCoverage` / `Cloud_GlobalDensity` scalars via a MID (range R4).** The engine `m_SimpleVolumetricCloud` (+ its `_Inst`) exposes scalar params `Cloud_GlobalCoverage`, `Cloud_GlobalDensity`, `Layout_CloudGlobalScale`, `StormClouds` and a `Cloud_AlbedoColor` vector (`inspect_material` to confirm). The component knobs (`LayerBottomAltitude`/`LayerHeight`) are NOT coverage — coverage is material-driven. Recipe (C++): `UVolumetricCloudComponent* CC = Cloud->FindComponentByClass<UVolumetricCloudComponent>(); UMaterialInterface* Base = CC->GetMaterial(); UMaterialInstanceDynamic* MID = UMaterialInstanceDynamic::Create(Base, this); CC->SetMaterial(MID); MID->SetScalarParameterValue("Cloud_GlobalCoverage", x);`. Caveat: the cloud component's `Material` is a `TSoftObjectPtr` and `GetMaterial()` returned **None** when read from editor Python (the `MaterialLifetimePtr` isn't resolved outside a registered render context) — it resolves fine at runtime in `BeginPlay`, but fall back to `LoadObject<UMaterialInterface>(nullptr, TEXT("/Engine/EngineSky/VolumetricClouds/m_SimpleVolumetricCloud_Inst"))` if null. Runtime light setters used alongside: `ULightComponent::{SetIntensity,SetUseTemperature,SetTemperature}` (via `ADirectionalLight::GetLightComponent()`), `UExponentialHeightFogComponent::SetFogDensity`, `USkyLightComponent::SetIntensity` (via `ASkyLight::GetLightComponent()`). Headers: `AVolumetricCloud` + `UVolumetricCloudComponent` both live in `Components/VolumetricCloudComponent.h`; light/fog actors in `Engine/{DirectionalLight,SkyLight,ExponentialHeightFog}.h`.
- **MCP `execute_console_command` runs in the EDITOR world, not the PIE world (range R4).** A console command registered with `FAutoConsoleCommandWithWorldAndArgs` receives the *editor* world when invoked via the bridge, so any command that finds a **PIE-spawned** actor fails: `golfsim.SetTime`/`golfsim.SetSky` (which `TActorIterator` for the PIE-only `AGolfRangeEnvironment`) logged `no AGolfRangeEnvironment in this level` even though it existed in PIE. They work when **typed in the PIE `~` console** (PIE world), or drive the feature through its UI (the HUD wires the dropdowns in the PIE world). For runtime verification of PIE-world state, read `get_log_lines` after a UI action rather than issuing the console command over MCP. (Same reason `golfsim.FireShot` over MCP would spawn its ball in the editor world.)
- **Adding a new .cpp can expose a latent missing `#include` in an UNRELATED file via unity-build reshuffling (range R4).** `GolfRangeHUD.cpp`'s R3 resolution readout used `UCanvas::SizeX/SizeY` without including `Engine/Canvas.h`; it had compiled only because the unity blob happened to pair it with a sibling that pulled Canvas in transitively. Adding `GolfRangeEnvironment.cpp` re-grouped the unity files, GolfRangeHUD landed alone, and the build failed `error C2027: use of undefined type 'UCanvas'` (plus a cascading `C7595` constexpr-format-string error from the now-unparseable `printf` args — fixing the include cleared both). Lesson: include what you use; a green build under unity is not proof the TU is self-contained. When a build suddenly fails in a file you didn't change, suspect a unity-grouping shift, not a regression in that file.
